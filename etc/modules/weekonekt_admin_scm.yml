version: 1.0
type: module
category: client
name: weekonekt_admin_scm 
spec:
  update:
    compile:
      cmds:
        - effect: scheme
          encoder: app
    scheme:
      model:
        scheme: 
          key: source 
      cmds:
        - effect: notify
          encoder: scheme
  encoders:
    app:
      object:
        app:
          spec: 
            modules:
              - ui_effects
              - account_ui
              - admin_common_ui
              - weekonekt_admin_reviews_ui
              - weekonekt_admin_import_ui
              - weekonekt_admin_places_ui
    scheme:
      data:
        key: scheme
  decoders:
    compile:
      object:
        method: 
          text:
            literal: "GET"
    scheme:
      object:
        language:
          keyword: scheme
        status:
          keyword: ok
        source:
          text: any
