version: 1.0
type: module
category: ui
name: nktest_home_ui 
spec:
  update:
    ready:
      cmds:
        - effect: ui
          encoder: test_reports_view
        - effect: ws
          encoder: test_reports
  decoders:
    ready:
      object:
        effect:
          keyword: events
        event:
          keyword: user_logged_in
