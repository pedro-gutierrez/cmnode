version: 1.0
type: module
category: client
name: admin_scm
priority: lowest
spec:
  update:
    compile:
      cmds:
        - effect: scheme
          encoder: app
    scheme:
      model:
        scheme: 
          key: source 
      cmds:
        - effect: notify
          encoder: scheme
  encoders:
    app:
      object:
        settings:
          config: settings
        app:
          spec: 
            modules:
              - ui_effects
              - account_ui
              - admin_common_ui
              - apps_ui
              - nodes_ui
              - ports_ui
              - buckets_ui
              - app_ui
              - module_ui
              - tracker_ui 
              - tests_ui
              - queues_ui
              - settings_ui
    scheme:
      data: 
        key: scheme
  decoders:
    compile:
      object:
        method:
          text: 
            literal: "GET"
    scheme:
      object:
        language:
          keyword: scheme
        status:
          keyword: ok
        source:
          text: any
