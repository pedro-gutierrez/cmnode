version: 1.0
type: module
category: client
name: common_ui_prefs
spec:
  init:
    model:
      prefs:
        lang: "en"
        filter: "dkv"
  decoders:
    prefs:
      object:
        effect: ws
        data:
          object:
            prefs:
              any: object
  update:
    prefs:
      model:
        prefs:
          key: prefs
          in: data
      cmds:
        - effect: events
          encoder: new_prefs
  encoders:
    prefs:
      object:
        action: prefs
    new_prefs:
      object:
        event: prefs
