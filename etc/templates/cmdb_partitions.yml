version: 1.0
type: template
name: cmdb_partitions
spec:
  contents: |-
    -module(cmdb_{{ name }}_partitions).
    -export([all/0, current/0]).
    
    current() -> 
      '{{ current }}'.

    all() ->
      [{% for p in partitions %}'{{ p }}'{% if not forloop.last %}, {% endif %}{% endfor %}].
